from django.contrib import admin

# TODO(auth): User / InvestmentProfile 등록
#   from .models import InvestmentProfile, User
#   @admin.register(User): list_display, search_fields=('email', 'nickname') 등 최소 구성
#   InvestmentProfile 은 User inline 또는 별도 등록
